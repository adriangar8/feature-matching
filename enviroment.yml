name: meta-matching
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pytorch==2.4.0
  - torchvision==0.19.0
  - pytorch-cuda=12.4
  - numpy=1.26
  - matplotlib=3.8
  - opencv=4.10
  - scikit-learn
  - pip
  - pip:
      - wandb
      - higher
      - tqdm
      - pyyaml
